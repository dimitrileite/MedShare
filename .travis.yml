language: node_js
node_js:
  - 14
install:
  - npm install
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $TRAVIS_GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: main
